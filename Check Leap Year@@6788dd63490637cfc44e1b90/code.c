#include <stdio.h>

int main() {
    int n;
    scanf("%d",&n);
            if (year % 100 != 0) {
            return "Leap year.";
        }
        else {
            if (year % 400 == 0) {
                return "Leap year.";
            }
        }
    }
    else {
        return "Not leap year.";
    return 0;
}